class PastEventsWidget extends WidgetBase { getName() { return 'past_events'; } getTitle() { return 'Past Events'; } getIcon() { return 'fa fa-calendar-check'; } getCategories() { return ['events']; } getKeywords() { return ['past', 'events', 'archive']; } registerControls() { this.startControlsSection('content_section', { label: 'Content', tab: 'content' }); this.addControl('title', { type: 'text', label: 'Title', default_value: 'Past Events', placeholder: 'Enter title', label_block: true }); this.endControlsSection(); this.startControlsSection('style_section', { label: 'Style', tab: 'style' }); this.addControl('accent_color', { type: 'color', label: 'Accent Color', default_value: '#6b7280' }); this.endControlsSection(); this.registerAdvancedControls(); } render() { const title = this.getSetting('title', 'Past Events'); const accentColor = this.getSetting('accent_color', '#6b7280'); const cssClasses = this.getSetting('css_classes', ''); const cssId = this.getSetting('css_id', ''); const animation = this.getSetting('animation', 'none'); const animationDuration = this.getSetting('animation_duration', { size: 0.5, unit: 's' }); const animationDelay = this.getSetting('animation_delay', { size: 0, unit: 's' }); const safeAnimationDuration = (animationDuration && typeof animationDuration === 'object' && animationDuration.size !== undefined && animationDuration.unit !== undefined) ? animationDuration : { size: 0.5, unit: 's' }; const safeAnimationDelay = (animationDelay && typeof animationDelay === 'object' && animationDelay.size !== undefined && animationDelay.unit !== undefined) ? animationDelay : { size: 0, unit: 's' }; const events = [{ title: 'Annual Conference 2023', date: 'Nov 15, 2023' }, { title: 'Product Launch', date: 'Oct 20, 2023' }].map(e => `<div style="border: 1px solid #e5e7eb; border-radius: 8px; padding: 15px; margin-bottom: 10px; opacity: 0.8;"><div style="font-size: 16px; font-weight: 700; margin-bottom: 5px;">${e.title}</div><div style="font-size: 14px; color: #666;"><i class="fa fa-calendar"></i> ${e.date}</div><a href="#" style="color: ${accentColor}; font-size: 14px; font-weight: 600; text-decoration: none; margin-top: 8px; display: inline-block;">View Recap <i class="fa fa-arrow-right"></i></a></div>`).join(''); const contentHtml = `<div><h3 style="font-size: 22px; font-weight: 700; margin: 0 0 20px 0;">${this.escapeHtml(title)}</h3>${events}</div>`; let wrapperClasses = ['past-events-widget']; if (cssClasses) wrapperClasses.push(cssClasses); if (animation !== 'none') wrapperClasses.push('animated', animation); let wrapperAttributes = ''; if (cssId) wrapperAttributes += ` id="${this.escapeHtml(cssId)}"`; let animationStyles = ''; if (animation !== 'none') { const duration = `${safeAnimationDuration.size}${safeAnimationDuration.unit}`; const delay = `${safeAnimationDelay.size}${safeAnimationDelay.unit}`; animationStyles = `animation-name: ${animation}; animation-duration: ${duration}; animation-delay: ${delay}; animation-fill-mode: both;`; } const wrapperStyle = animationStyles ? ` style="${animationStyles.trim()}"` : ''; return `<div class="${wrapperClasses.join(' ')}"${wrapperAttributes}${wrapperStyle}>${contentHtml}</div>`; } escapeHtml(text) { const div = document.createElement('div'); div.textContent = text; return div.innerHTML; } }

window.elementorWidgetManager.registerWidget(PastEventsWidget);
